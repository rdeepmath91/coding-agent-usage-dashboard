From d7cf2bd4c4d8b8f3b6f9c6e331d88f14b83c68fa Mon Sep 17 00:00:00 2001
From: Raymond Christopher <raymond.christopher@gdplabs.id>
Date: Thu, 28 May 2026 12:30:53 +0700
Subject: [PATCH 1/4] refine: issue #5 source copy and chart filtering UX

---
 app.py               |  38 ++++++++++-
 pyproject.toml       |   3 +
 templates/index.html | 150 +++++++++++++++++++++++++++++++++----------
 tests/test_app.py    |  16 +++++
 4 files changed, 169 insertions(+), 38 deletions(-)

diff --git a/app.py b/app.py
index e231641..2cfbb6e 100644
--- a/app.py
+++ b/app.py
@@ -31,9 +31,10 @@ TOOL_SOURCES = [
         "id": "opencode",
         "label": "OpenCode",
         "status": "active",
-        "status_label": "Active",
+        "status_label": "Active source",
         "source_type": "SQLite database",
         "source_path": display_path(DB_PATH),
+        "repo_url": "https://github.com/anomalyco/opencode/",
         "color": "#3B82F6",
         "issue": None,
     },
@@ -41,9 +42,10 @@ TOOL_SOURCES = [
         "id": "codex",
         "label": "Codex CLI",
         "status": "placeholder",
-        "status_label": "Planned",
+        "status_label": "Planned adapter",
         "source_type": "Local session/history data",
         "source_path": "TBD",
+        "repo_url": "https://github.com/openai/codex/",
         "color": "#BA68C8",
         "issue": None,
     },
@@ -51,9 +53,10 @@ TOOL_SOURCES = [
         "id": "hermes",
         "label": "Hermes",
         "status": "placeholder",
-        "status_label": "Planned",
+        "status_label": "Planned adapter",
         "source_type": "Local session/tool logs or session DB",
         "source_path": "TBD",
+        "repo_url": "https://github.com/NousResearch/hermes-agent/",
         "color": "#EAB308",
         "issue": None,
     },
@@ -221,6 +224,28 @@ def parse_top_n(default: int = 8) -> int:
     return max(3, min(raw or default, len(QUALITATIVE_COLORS)))
 
 
+def tool_source_label(tool_id: str | None) -> str | None:
+    if not tool_id:
+        return None
+    for source in current_tool_sources():
+        if source["id"] == tool_id:
+            return source["label"]
+    return tool_id
+
+
+def empty_daily_response(top_n: int, selected_tool_id: str | None):
+    return jsonify({
+        "dates": [],
+        "models": [],
+        "data": {},
+        "top_n": top_n,
+        "other_count": 0,
+        "selected_model_id": None,
+        "selected_tool_id": selected_tool_id,
+        "selected_tool_label": tool_source_label(selected_tool_id),
+    })
+
+
 def simulate_enabled() -> bool:
     raw = request.args.get("simulate") or request.args.get("demo") or os.environ.get("DASHBOARD_SIMULATE")
     if raw is None:
@@ -642,6 +667,9 @@ def api_daily():
     days = parse_days(default=31)
     top_n = parse_top_n(default=8)
     selected_model_id = request.args.get("model_id") or None
+    selected_tool_id = request.args.get("tool_id") or None
+    if selected_tool_id and selected_tool_id != "opencode":
+        return empty_daily_response(top_n, selected_tool_id)
     if simulate_enabled():
         simulated = build_simulated_dataset(days)
         daily_data = simulated["daily"]
@@ -716,6 +744,8 @@ def api_daily():
             "top_n": top_n,
             "other_count": 0 if selected_model_id else len(other_models),
             "selected_model_id": selected_model_id if selected_model_id in active_models else None,
+            "selected_tool_id": selected_tool_id,
+            "selected_tool_label": tool_source_label(selected_tool_id),
         })
     where, params = since_clause(days)
     msg_where = where.replace("time_created", "m.time_created")
@@ -860,6 +890,8 @@ def api_daily():
         "top_n": top_n,
         "other_count": 0 if selected_model_id else len(other_models),
         "selected_model_id": selected_model_id if selected_model_id in active_models else None,
+        "selected_tool_id": selected_tool_id,
+        "selected_tool_label": tool_source_label(selected_tool_id),
     })
 
 
diff --git a/pyproject.toml b/pyproject.toml
index ede3707..d026ea6 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -10,3 +10,6 @@ dependencies = [
 
 [tool.uv]
 package = false
+
+[tool.pytest.ini_options]
+pythonpath = ["."]
diff --git a/templates/index.html b/templates/index.html
index 1736187..ee6b22f 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -273,23 +273,41 @@
     border: 1px solid var(--border);
     border-radius: var(--radius);
     padding: 14px 16px;
-    min-height: 118px;
+    min-height: 104px;
+    cursor: pointer;
   }
   .tool-source-card.placeholder {
     border-style: dashed;
     opacity: 0.78;
   }
+  .tool-source-card.active-filter {
+    background: rgb(33 150 243 / 0.08);
+  }
   .tool-source-head {
     display: flex;
     justify-content: space-between;
     gap: 10px;
-    align-items: baseline;
+    align-items: flex-start;
     margin-bottom: 8px;
   }
-  .tool-source-name { font-weight: 600; }
+  .tool-source-name {
+    border: 0;
+    background: transparent;
+    padding: 0;
+    color: inherit;
+    cursor: pointer;
+    font: inherit;
+    font-weight: 600;
+    text-align: left;
+  }
+  .tool-source-name:hover { text-decoration: underline; text-underline-offset: 3px; }
+  .tool-source-name:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; border-radius: 2px; }
   .tool-source-status { font-family: var(--font-mono); font-size: 11px; color: var(--text-tertiary); text-transform: uppercase; letter-spacing: 0.4px; }
-  .tool-source-value { font-size: 20px; font-weight: 700; font-variant-numeric: tabular-nums; }
   .tool-source-meta { margin-top: 6px; color: var(--text-secondary); font-family: var(--font-mono); font-size: 12px; line-height: 1.5; }
+  .tool-source-actions { display: flex; align-items: center; gap: 10px; margin-top: 6px; font-size: 12px; }
+  .tool-source-filter-state { color: var(--text-tertiary); }
+  .tool-source-repo { color: var(--accent); text-decoration: none; }
+  .tool-source-repo:hover { color: var(--accent-hover); text-decoration: underline; text-underline-offset: 3px; }
   .tool-pill {
     display: inline-flex;
     align-items: center;
@@ -634,7 +652,7 @@
   <main class="main-area">
     <section id="cost" class="page-header">
       <h1>Overview</h1>
-      <div class="subtitle">Sessions and tokens broken down by model. Cache is shown as secondary context.</div>
+      <div class="subtitle">Primary totals use assistant-message input + output tokens. Cache read/write is tracked separately.</div>
     </section>
 
     <!-- Pill tabs -->
@@ -701,7 +719,7 @@
         </label>
       </div>
       <div class="chart-filter-bar" id="chart-filter-bar" aria-live="polite">
-        <span>Chart focused on <span class="chart-filter-model" id="chart-filter-model">one model</span></span>
+        <span>Chart focused on <span class="chart-filter-model" id="chart-filter-model">one selection</span></span>
         <button class="chart-filter-clear" id="clear-chart-filter" type="button">Clear filter</button>
       </div>
       <div class="chart-layout">
@@ -770,6 +788,8 @@ let usageChart = null;
 let currentTopN = 8;
 let selectedChartModelId = null;
 let selectedChartModelLabel = null;
+let selectedChartToolId = null;
+let selectedChartToolLabel = null;
 const hiddenChartModels = new Set();
 const FORMATTER = new Intl.NumberFormat('en-US');
 function fmtNum(n) { return FORMATTER.format(n); }
@@ -848,7 +868,7 @@ function setCardMeta(id, visible, detail = '') {
         <button
           type="button"
           class="meta-info"
-          aria-label="More information"
+          aria-label="More information: ${escapeHtml(visible)}"
           aria-expanded="false"
           aria-describedby="${escapeHtml(tooltipId)}"
         >i</button>
@@ -900,11 +920,16 @@ function renderToolSources(sources = []) {
 
   sources.forEach(source => {
     const active = source.status === 'active';
-    const value = active && source.tokens_total !== null ? fmtTokens(source.tokens_total || 0) : '—';
     const color = sanitizeColor(source.color, '#64748B');
 
     const card = document.createElement('article');
     card.className = `tool-source-card ${active ? 'active' : 'placeholder'}`;
+    card.dataset.toolId = source.id || '';
+    card.title = source.repo_url ? `Repo: ${source.repo_url}` : '';
+    card.addEventListener('click', event => {
+      if (event.target.closest('a, button')) return;
+      setChartToolFilter(source.id, source.label || source.id);
+    });
     if (active) {
       card.style.borderColor = color;
       card.style.borderLeft = `3px solid ${color}`;
@@ -914,25 +939,45 @@ function renderToolSources(sources = []) {
     const head = document.createElement('div');
     head.className = 'tool-source-head';
 
-    const name = document.createElement('span');
+    const name = document.createElement('button');
+    name.type = 'button';
     name.className = 'tool-source-name';
     name.style.color = color;
     name.textContent = source.label || 'Unknown';
+    name.setAttribute('aria-pressed', selectedChartToolId === source.id ? 'true' : 'false');
+    name.setAttribute('aria-label', `Focus chart on ${source.label || 'selected source'} source`);
+    name.addEventListener('click', () => setChartToolFilter(source.id, source.label || source.id));
 
     const status = document.createElement('span');
     status.className = 'tool-source-status';
     status.textContent = source.status_label || '';
 
-    const valueEl = document.createElement('div');
-    valueEl.className = 'tool-source-value';
-    valueEl.textContent = value;
-
     const meta = document.createElement('div');
     meta.className = 'tool-source-meta';
-    meta.textContent = active ? String(source.source_path || '') : String(source.source_path || '');
+    meta.textContent = source.source_type || 'Source type unknown';
+
+    const actions = document.createElement('div');
+    actions.className = 'tool-source-actions';
+
+    const filterState = document.createElement('span');
+    filterState.className = 'tool-source-filter-state';
+    filterState.textContent = selectedChartToolId === source.id ? 'chart focused' : 'click name to filter';
+    actions.appendChild(filterState);
+
+    if (source.repo_url) {
+      const repo = document.createElement('a');
+      repo.className = 'tool-source-repo';
+      repo.href = source.repo_url;
+      repo.target = '_blank';
+      repo.rel = 'noopener noreferrer';
+      repo.textContent = 'repo';
+      repo.title = source.repo_url;
+      repo.setAttribute('aria-label', `${source.label || 'Tool'} upstream repository`);
+      actions.appendChild(repo);
+    }
 
     head.append(name, status);
-    card.append(head, valueEl, meta);
+    card.append(head, meta, actions);
     grid.appendChild(card);
   });
 }
@@ -1011,20 +1056,20 @@ async function loadOverview(days) {
     );
     setCardMeta(
       'tokens-sub',
-      `${totalAvg} avg/session`,
-      `${d.token_total_definition || 'Total tokens are input + output.'} Source: ${sourceLabel} only. Cache read/write is shown as secondary context, not included in total tokens.`,
+      `${fmtTokens(d.cache_read || 0)} read / ${fmtTokens(d.cache_write || 0)} write cache`,
+      `${d.token_total_definition || 'Total tokens are input + output.'} ${totalAvg} avg/session. Source: ${sourceLabel} only.`,
     );
     setCardMeta(
       'input-sub',
-      `${inputAvg} avg/session`,
-      `${fmtTokens(d.cache_read || 0)} cache read. Reused context is shown separately from input tokens.`,
+      `${fmtTokens(d.cache_read || 0)} cache read`,
+      `Input tokens are prompt/context tokens sent to the model. Cache read is reused context and is excluded from input totals. ${inputAvg} input avg/session.`,
     );
     setCardMeta(
       'output-sub',
-      `${outputAvg} avg/session`,
-      `${fmtTokens(d.cache_write || 0)} cache write. Cache created is shown separately from output tokens.`,
+      `${fmtTokens(d.cache_write || 0)} cache write`,
+      `Output tokens are assistant response tokens. Cache write is newly cached context and is excluded from output totals. ${outputAvg} output avg/session.`,
     );
-    document.getElementById('tool-sources-subtitle').textContent = `Totals currently come from ${sourceLabel} (${sourcePath}). ${d.token_total_definition || 'Total tokens are input + output.'}`;
+    document.getElementById('tool-sources-subtitle').textContent = `Click a source to focus the chart. Totals currently come from ${sourceLabel}.`;
     renderToolSources(d.tool_sources || []);
   } catch(e) {
     console.error('overview', e);
@@ -1048,9 +1093,9 @@ async function loadModels(days) {
     document.getElementById('estimated-cost').textContent = priced.length ? fmtCost(estimatedTotal) : '—';
     setCardMeta(
       'estimated-cost-sub',
-      priced.length ? `${priced.length}/${models.length} priced` : 'No matched pricing',
+      priced.length ? `approx · ${priced.length}/${models.length} priced` : 'No matched pricing',
       priced.length
-        ? `${unpricedCount} unmatched models. Cost uses live OpenRouter pricing first, then hardcoded fallback pricing for known OpenAI and opencode/go models.`
+        ? `${unpricedCount} unmatched models. Estimate uses live OpenRouter pricing first, then fallback pricing for known OpenAI and opencode/go models.`
         : 'No OpenRouter or hardcoded pricing matched the active models.',
     );
 
@@ -1124,16 +1169,21 @@ async function loadModels(days) {
 
 async function loadDaily(days) {
   try {
-    const selectedParam = selectedChartModelId ? `&model_id=${encodeURIComponent(selectedChartModelId)}` : '';
-    const r = await fetch(`/api/daily?days=${rangeParam()}&top_n=${currentTopN}${selectedParam}${simulateQuerySuffix()}`);
+    const selectedModelParam = selectedChartModelId ? `&model_id=${encodeURIComponent(selectedChartModelId)}` : '';
+    const selectedToolParam = selectedChartToolId ? `&tool_id=${encodeURIComponent(selectedChartToolId)}` : '';
+    const r = await fetch(`/api/daily?days=${rangeParam()}&top_n=${currentTopN}${selectedModelParam}${selectedToolParam}${simulateQuerySuffix()}`);
     const data = await r.json();
 
-    syncChartModelSelection(data);
+    syncChartSelection(data);
     renderChart(data);
     renderLegend(data.models);
     updateChartFilterUi();
-    document.getElementById('chart-note').textContent = selectedChartModelId
+    document.getElementById('chart-note').textContent = selectedChartToolId && !data.models.length
+      ? `No connected ${selectedChartToolLabel || 'source'} data for this range`
+      : selectedChartModelId
       ? 'Single-model focus · click Clear filter to restore Top N view'
+      : selectedChartToolId
+      ? `Source focus: ${selectedChartToolLabel || selectedChartToolId} · click Clear filter to restore all sources`
       : `Position-based colors · top ${data.top_n} models by token volume · ${data.other_count || 0} long-tail models grouped as Other`;
   } catch(e) {
     console.error('daily', e);
@@ -1144,8 +1194,8 @@ function updateChartFilterUi() {
   const bar = document.getElementById('chart-filter-bar');
   const label = document.getElementById('chart-filter-model');
   if (!bar || !label) return;
-  bar.classList.toggle('active', Boolean(selectedChartModelId));
-  label.textContent = selectedChartModelLabel || 'selected model';
+  bar.classList.toggle('active', Boolean(selectedChartModelId || selectedChartToolId));
+  label.textContent = selectedChartModelLabel || selectedChartToolLabel || 'selection';
   document.querySelectorAll('.model-filter-row').forEach(row => {
     const active = row.dataset.chartModelId === selectedChartModelId;
     row.classList.toggle('active', active);
@@ -1154,23 +1204,51 @@ function updateChartFilterUi() {
     button.setAttribute('aria-pressed', active ? 'true' : 'false');
     button.textContent = active ? 'Clear focus' : 'Focus chart';
   });
+  document.querySelectorAll('.tool-source-card').forEach(card => {
+    const active = card.dataset.toolId === selectedChartToolId;
+    card.classList.toggle('active-filter', active);
+    const button = card.querySelector('.tool-source-name');
+    button?.setAttribute('aria-pressed', active ? 'true' : 'false');
+    const state = card.querySelector('.tool-source-filter-state');
+    if (state) state.textContent = active ? 'chart focused' : 'click name to filter';
+  });
 }
 
-function syncChartModelSelection(data) {
+function syncChartSelection(data) {
   if (!data?.selected_model_id) {
     selectedChartModelId = null;
     selectedChartModelLabel = null;
-    return;
+  } else {
+    selectedChartModelId = data.selected_model_id;
+    const selectedMeta = (data.models || []).find(model => model.id === data.selected_model_id);
+    selectedChartModelLabel = selectedMeta?.label || selectedChartModelLabel || data.selected_model_id;
+  }
+  if (!data?.selected_tool_id) {
+    selectedChartToolId = null;
+    selectedChartToolLabel = null;
+  } else {
+    selectedChartToolId = data.selected_tool_id;
+    selectedChartToolLabel = data.selected_tool_label || selectedChartToolLabel || data.selected_tool_id;
   }
-  selectedChartModelId = data.selected_model_id;
-  const selectedMeta = (data.models || []).find(model => model.id === data.selected_model_id);
-  selectedChartModelLabel = selectedMeta?.label || selectedChartModelLabel || data.selected_model_id;
 }
 
 function setChartModelFilter(modelId, label) {
   const sameModel = selectedChartModelId === modelId;
   selectedChartModelId = sameModel ? null : modelId;
   selectedChartModelLabel = sameModel ? null : label;
+  selectedChartToolId = null;
+  selectedChartToolLabel = null;
+  hiddenChartModels.clear();
+  loadDaily(currentDays);
+  updateChartFilterUi();
+}
+
+function setChartToolFilter(toolId, label) {
+  const sameTool = selectedChartToolId === toolId;
+  selectedChartToolId = sameTool ? null : toolId;
+  selectedChartToolLabel = sameTool ? null : label;
+  selectedChartModelId = null;
+  selectedChartModelLabel = null;
   hiddenChartModels.clear();
   loadDaily(currentDays);
   updateChartFilterUi();
@@ -1179,6 +1257,8 @@ function setChartModelFilter(modelId, label) {
 function clearChartModelFilter() {
   selectedChartModelId = null;
   selectedChartModelLabel = null;
+  selectedChartToolId = null;
+  selectedChartToolLabel = null;
   hiddenChartModels.clear();
   loadDaily(currentDays);
   updateChartFilterUi();
diff --git a/tests/test_app.py b/tests/test_app.py
index a35d4ba..5889f9d 100644
--- a/tests/test_app.py
+++ b/tests/test_app.py
@@ -199,17 +199,33 @@ class DashboardApiTests(unittest.TestCase):
 
         sources = {item['id']: item for item in payload['tool_sources']}
         self.assertEqual(sources['opencode']['status'], 'active')
+        self.assertEqual(sources['opencode']['status_label'], 'Active source')
         self.assertEqual(sources['opencode']['color'], '#3B82F6')
         self.assertEqual(sources['opencode']['source_path'], display_like_app(self.db_path))
+        self.assertEqual(sources['opencode']['repo_url'], 'https://github.com/anomalyco/opencode/')
         self.assertEqual(sources['codex']['status'], 'placeholder')
+        self.assertEqual(sources['codex']['status_label'], 'Planned adapter')
         self.assertEqual(sources['codex']['color'], '#BA68C8')
         self.assertEqual(sources['codex']['source_path'], 'TBD')
+        self.assertEqual(sources['codex']['repo_url'], 'https://github.com/openai/codex/')
         self.assertIsNone(sources['codex']['issue'])
         self.assertEqual(sources['hermes']['status'], 'placeholder')
+        self.assertEqual(sources['hermes']['status_label'], 'Planned adapter')
         self.assertEqual(sources['hermes']['color'], '#EAB308')
         self.assertEqual(sources['hermes']['source_path'], 'TBD')
+        self.assertEqual(sources['hermes']['repo_url'], 'https://github.com/NousResearch/hermes-agent/')
         self.assertIsNone(sources['hermes']['issue'])
 
+    def test_planned_tool_source_filter_returns_empty_daily_result(self):
+        response = self.client.get('/api/daily?days=30&tool_id=codex')
+        self.assertEqual(response.status_code, 200)
+        payload = response.get_json()
+
+        self.assertEqual(payload['selected_tool_id'], 'codex')
+        self.assertEqual(payload['selected_tool_label'], 'Codex CLI')
+        self.assertEqual(payload['models'], [])
+        self.assertEqual(payload['data'], {})
+
     def test_models_and_history_include_tool_color(self):
         models_response = self.client.get('/api/models?days=30')
         self.assertEqual(models_response.status_code, 200)
-- 
2.43.0


From 26cfe64d2451801b838277fb986747ee60ea8c61 Mon Sep 17 00:00:00 2001
From: Raymond Christopher <raymond.christopher@gdplabs.id>
Date: Thu, 28 May 2026 16:53:54 +0700
Subject: [PATCH 2/4] fix: expose source_path in tool cards and tighten daily
 tool filter errors

---
 app.py               | 17 +++++++++++++++--
 pyproject.toml       |  3 ---
 templates/index.html |  8 ++++++--
 tests/test_app.py    | 19 +++++++++++++++++++
 4 files changed, 40 insertions(+), 7 deletions(-)

diff --git a/app.py b/app.py
index 2cfbb6e..a425b46 100644
--- a/app.py
+++ b/app.py
@@ -63,6 +63,7 @@ TOOL_SOURCES = [
 ]
 
 TOOL_COLOR_MAP = {t["id"]: t["color"] for t in TOOL_SOURCES}
+KNOWN_TOOL_IDS = {t["id"] for t in TOOL_SOURCES}
 
 
 def current_tool_sources() -> list[dict]:
@@ -233,7 +234,7 @@ def tool_source_label(tool_id: str | None) -> str | None:
     return tool_id
 
 
-def empty_daily_response(top_n: int, selected_tool_id: str | None):
+def empty_daily_response(top_n: int, selected_tool_id: str | None, error_message: str | None = None):
     return jsonify({
         "dates": [],
         "models": [],
@@ -243,6 +244,7 @@ def empty_daily_response(top_n: int, selected_tool_id: str | None):
         "selected_model_id": None,
         "selected_tool_id": selected_tool_id,
         "selected_tool_label": tool_source_label(selected_tool_id),
+        "error": error_message,
     })
 
 
@@ -669,7 +671,18 @@ def api_daily():
     selected_model_id = request.args.get("model_id") or None
     selected_tool_id = request.args.get("tool_id") or None
     if selected_tool_id and selected_tool_id != "opencode":
-        return empty_daily_response(top_n, selected_tool_id)
+        if selected_tool_id in KNOWN_TOOL_IDS:
+            source_label = tool_source_label(selected_tool_id) or selected_tool_id
+            return empty_daily_response(
+                top_n,
+                selected_tool_id,
+                error_message=f"{source_label} is planned and not connected yet.",
+            )
+        return empty_daily_response(
+            top_n,
+            None,
+            error_message=f"Unsupported tool_id: {selected_tool_id}.",
+        )
     if simulate_enabled():
         simulated = build_simulated_dataset(days)
         daily_data = simulated["daily"]
diff --git a/pyproject.toml b/pyproject.toml
index d026ea6..ede3707 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -10,6 +10,3 @@ dependencies = [
 
 [tool.uv]
 package = false
-
-[tool.pytest.ini_options]
-pythonpath = ["."]
diff --git a/templates/index.html b/templates/index.html
index ee6b22f..5c59550 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -954,7 +954,9 @@ function renderToolSources(sources = []) {
 
     const meta = document.createElement('div');
     meta.className = 'tool-source-meta';
-    meta.textContent = source.source_type || 'Source type unknown';
+    const sourceType = source.source_type || 'Source type unknown';
+    const sourcePath = source.source_path || 'Unknown';
+    meta.textContent = `${sourceType} · Source: ${sourcePath}`;
 
     const actions = document.createElement('div');
     actions.className = 'tool-source-actions';
@@ -1178,7 +1180,9 @@ async function loadDaily(days) {
     renderChart(data);
     renderLegend(data.models);
     updateChartFilterUi();
-    document.getElementById('chart-note').textContent = selectedChartToolId && !data.models.length
+    document.getElementById('chart-note').textContent = data.error
+      ? data.error
+      : selectedChartToolId && !data.models.length
       ? `No connected ${selectedChartToolLabel || 'source'} data for this range`
       : selectedChartModelId
       ? 'Single-model focus · click Clear filter to restore Top N view'
diff --git a/tests/test_app.py b/tests/test_app.py
index 5889f9d..fe8ce5a 100644
--- a/tests/test_app.py
+++ b/tests/test_app.py
@@ -225,6 +225,18 @@ class DashboardApiTests(unittest.TestCase):
         self.assertEqual(payload['selected_tool_label'], 'Codex CLI')
         self.assertEqual(payload['models'], [])
         self.assertEqual(payload['data'], {})
+        self.assertEqual(payload['error'], 'Codex CLI is planned and not connected yet.')
+
+    def test_unknown_tool_source_filter_returns_empty_error_contract(self):
+        response = self.client.get('/api/daily?days=30&tool_id=unknown')
+        self.assertEqual(response.status_code, 200)
+        payload = response.get_json()
+
+        self.assertIsNone(payload['selected_tool_id'])
+        self.assertIsNone(payload['selected_tool_label'])
+        self.assertEqual(payload['models'], [])
+        self.assertEqual(payload['data'], {})
+        self.assertEqual(payload['error'], 'Unsupported tool_id: unknown.')
 
     def test_models_and_history_include_tool_color(self):
         models_response = self.client.get('/api/models?days=30')
@@ -269,6 +281,13 @@ class DashboardApiTests(unittest.TestCase):
         self.assertIn("button.textContent = active ? 'Clear focus' : 'Focus chart'", html)
         self.assertNotIn('role="button" data-chart-model-id', html)
 
+    def test_tool_source_render_includes_path_handling(self):
+        response = self.client.get('/')
+        self.assertEqual(response.status_code, 200)
+        html = response.get_data(as_text=True)
+        self.assertIn('const sourcePath = source.source_path || \'Unknown\'', html)
+        self.assertIn('Source: ${sourcePath}', html)
+
     def test_simulated_mode_returns_synthetic_dashboard_data(self):
         overview = self.client.get('/api/overview?simulate=1&days=31')
         self.assertEqual(overview.status_code, 200)
-- 
2.43.0


From 9b9b3aa14e166893cd224e9db6a153e2f6b9b2dd Mon Sep 17 00:00:00 2001
From: Raymond Christopher <raymond.christopher@gdplabs.id>
Date: Thu, 28 May 2026 18:02:37 +0700
Subject: [PATCH 3/4] fix: return 400 for unknown daily tool filters

---
 app.py            | 17 +++++++++++++----
 tests/test_app.py |  6 ++----
 2 files changed, 15 insertions(+), 8 deletions(-)

diff --git a/app.py b/app.py
index a425b46..d7a48cb 100644
--- a/app.py
+++ b/app.py
@@ -678,10 +678,19 @@ def api_daily():
                 selected_tool_id,
                 error_message=f"{source_label} is planned and not connected yet.",
             )
-        return empty_daily_response(
-            top_n,
-            None,
-            error_message=f"Unsupported tool_id: {selected_tool_id}.",
+        return (
+            jsonify({
+                "error": f"Unsupported tool_id: {selected_tool_id}.",
+                "dates": [],
+                "models": [],
+                "data": {},
+                "top_n": top_n,
+                "other_count": 0,
+                "selected_model_id": None,
+                "selected_tool_id": None,
+                "selected_tool_label": None,
+            }),
+            400,
         )
     if simulate_enabled():
         simulated = build_simulated_dataset(days)
diff --git a/tests/test_app.py b/tests/test_app.py
index fe8ce5a..2d6ec00 100644
--- a/tests/test_app.py
+++ b/tests/test_app.py
@@ -227,13 +227,11 @@ class DashboardApiTests(unittest.TestCase):
         self.assertEqual(payload['data'], {})
         self.assertEqual(payload['error'], 'Codex CLI is planned and not connected yet.')
 
-    def test_unknown_tool_source_filter_returns_empty_error_contract(self):
+    def test_unknown_tool_source_filter_returns_http_400(self):
         response = self.client.get('/api/daily?days=30&tool_id=unknown')
-        self.assertEqual(response.status_code, 200)
+        self.assertEqual(response.status_code, 400)
         payload = response.get_json()
 
-        self.assertIsNone(payload['selected_tool_id'])
-        self.assertIsNone(payload['selected_tool_label'])
         self.assertEqual(payload['models'], [])
         self.assertEqual(payload['data'], {})
         self.assertEqual(payload['error'], 'Unsupported tool_id: unknown.')
-- 
2.43.0


From 675a17a64be1f04ef1ef7b1993a5308ead202f36 Mon Sep 17 00:00:00 2001
From: Raymond Christopher <raymond.christopher@gdplabs.id>
Date: Thu, 28 May 2026 18:05:59 +0700
Subject: [PATCH 4/4] fix: guard daily chart fetch failures in UI

---
 templates/index.html | 51 +++++++++++++++++++++++++++++++++++++-------
 tests/test_app.py    |  2 ++
 2 files changed, 45 insertions(+), 8 deletions(-)

diff --git a/templates/index.html b/templates/index.html
index 5c59550..de3ad0c 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -1174,23 +1174,58 @@ async function loadDaily(days) {
     const selectedModelParam = selectedChartModelId ? `&model_id=${encodeURIComponent(selectedChartModelId)}` : '';
     const selectedToolParam = selectedChartToolId ? `&tool_id=${encodeURIComponent(selectedChartToolId)}` : '';
     const r = await fetch(`/api/daily?days=${rangeParam()}&top_n=${currentTopN}${selectedModelParam}${selectedToolParam}${simulateQuerySuffix()}`);
-    const data = await r.json();
+    const data = await r.json().catch(() => null);
+
+    if (!r.ok) {
+      const fallback = {
+        dates: [],
+        models: [],
+        data: {},
+        top_n: currentTopN,
+        other_count: 0,
+      };
+      const chartData = data && typeof data === 'object' ? data : fallback;
+      syncChartSelection(chartData);
+      renderChart(fallback);
+      renderLegend([]);
+      updateChartFilterUi();
+      document.getElementById('chart-note').textContent = chartData.error
+        ? chartData.error
+        : `Request failed with ${r.status} ${r.statusText || ''}`.trim();
+      return;
+    }
 
-    syncChartSelection(data);
-    renderChart(data);
-    renderLegend(data.models);
+    const safeData = data || {
+      dates: [],
+      models: [],
+      data: {},
+      top_n: currentTopN,
+      other_count: 0,
+      selected_model_id: null,
+      selected_tool_id: null,
+      selected_tool_label: null,
+      error: null,
+    };
+
+    syncChartSelection(safeData);
+    renderChart(safeData);
+    renderLegend(safeData.models);
     updateChartFilterUi();
-    document.getElementById('chart-note').textContent = data.error
-      ? data.error
-      : selectedChartToolId && !data.models.length
+    document.getElementById('chart-note').textContent = safeData.error
+      ? safeData.error
+      : selectedChartToolId && !safeData.models.length
       ? `No connected ${selectedChartToolLabel || 'source'} data for this range`
       : selectedChartModelId
       ? 'Single-model focus · click Clear filter to restore Top N view'
       : selectedChartToolId
       ? `Source focus: ${selectedChartToolLabel || selectedChartToolId} · click Clear filter to restore all sources`
-      : `Position-based colors · top ${data.top_n} models by token volume · ${data.other_count || 0} long-tail models grouped as Other`;
+      : `Position-based colors · top ${safeData.top_n} models by token volume · ${safeData.other_count || 0} long-tail models grouped as Other`;
   } catch(e) {
     console.error('daily', e);
+    renderChart({ dates: [], models: [], data: {}, top_n: currentTopN, other_count: 0 });
+    renderLegend([]);
+    updateChartFilterUi();
+    document.getElementById('chart-note').textContent = 'Failed to load chart data.';
   }
 }
 
diff --git a/tests/test_app.py b/tests/test_app.py
index 2d6ec00..b5a3fc6 100644
--- a/tests/test_app.py
+++ b/tests/test_app.py
@@ -278,6 +278,8 @@ class DashboardApiTests(unittest.TestCase):
         self.assertIn('item.className = `legend-item', html)
         self.assertIn("button.textContent = active ? 'Clear focus' : 'Focus chart'", html)
         self.assertNotIn('role="button" data-chart-model-id', html)
+        self.assertIn('if (!r.ok)', html)
+        self.assertIn("document.getElementById('chart-note').textContent = chartData.error", html)
 
     def test_tool_source_render_includes_path_handling(self):
         response = self.client.get('/')
-- 
2.43.0

