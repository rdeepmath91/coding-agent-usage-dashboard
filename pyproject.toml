[project]
name = "coding-agent-usage-dashboard"
version = "0.1.0"
description = "Local dashboard for inspecting coding-agent usage from your machine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "flask>=3.0",
]

[tool.uv]
package = false
